// import axios from "axios";

//* 12. Mодуль 12.Синтаксис async/await. Пагінація

//!    Синтаксис async/await

//? Асинхронні функції

// Синтаксис async/await — це зручний спосіб написання асинхронного коду, який виглядає схожим на синхронний. Цей підхід був доданий 
// у стандарт ECMAScript 2017 (ES8) і став популярним серед розробників завдяки простоті та зрозумілості.
// Будь-яка функція може бути асинхронною, якщо перед її оголошенням додати ключове слово async.
// Для оголошення асинхронної функції перед ключовим словом function додається async.

// async function fetchData() {
// 	// ...
// }У функціональному виразі також перед ключовим словом function додається async.

// const fetchData = async function() {
// 	// ...
// }

// Для оголошення асинхронної стрілкової функції перед списком параметрів додається ключове слово async.

// const fetchData = async () => {
// 	// ...
// }

// Для оголошення асинхронного методу об'єкта async додається перед іменем методу.

// const user = {
//   async getUsername() {
//     // ...
//   },
// };

// Для оголошення асинхронного методу класу також використовується async перед іменем методу.

// class User {
//   async getUsername() {
//     // ...
//   }
// }

// Асинхронні функції дозволяють працювати з await, що робить код чистішим та легшим для розуміння, усуваючи необхідність у вкладених .then().

// todo test:
// Яке ключове слово використовується для оголошення асинхронної функції?

// makeAasync
// await
//* async
// then
// Result

//* І це правильна відповідь! Ключове слово, яке використовується для оголошення асинхронної функції, це async.

//? Значення, що повертається

// Синтаксис async/await базується на промісах, тому не блокує основний потік виконання програми. Це альтернатива підходу з 
// then/catch для роботи з асинхронним кодом.
// Що повертає асинхронна функція?
// Завжди повертає проміс.
// Якщо функція повертає значення, воно обгортається в проміс.
// Якщо не повертає значення, створюється проміс із undefined.
// Отже, будь-яке значення, що повертається з функції, стане значенням проміса.
// const foo = async () => {
// 	return 5;
// };

// foo().then(value => {
// 	console.log(value); // 5
// });

// Функція foo() повертає 5, але фактично це Promise.resolve(5).
// Якщо не вказувати значення для повернення, буде створений проміс зі значенням undefined.

// const foo = async () => {
// 	// Не вказуємо значення для повернення
// };

// foo().then(value => {
// 	console.log(value); // undefined
// });

// Оскільки нічого не повертається, результатом буде Promise.resolve(undefined).
// Асинхронні функції спрощують роботу з промісами, дозволяючи уникнути вкладених .then() і роблячи код читабельнішим.
// todo test:
// Що повертає асинхронна функція?

//* Завжди проміс
// Значення, яке вказане після return
// undefined
// Result

//* Звісно! Асинхронна функція завжди повертає проміс.

//? Оператор await

// Оператор await використовується всередині асинхронних функцій (async) для очікування завершення промісу перед тим, як виконання коду продовжиться.
// Коли інтерпретатор зустрічає await:
// Виконання функції призупиняється до завершення промісу.
// Якщо проміс виконано успішно (fulfilled), await повертає його значення.
// Якщо проміс відхилено (rejected), виникає помилка.

// const foo = async () => {
// 	console.log("Before await");

// 	const promiseValue = await new Promise(resolve => {
// 		setTimeout(() => resolve(5), 2000)
// 	});	

// 	console.log("After await", promiseValue);
// };

// foo(); // через 2 секунди виведеться в консоль  "After await" 5



// Що відбувається в коді?
// console.log("Before await") виконується синхронно.
// Інтерпретатор зустрічає await та призупиняє виконання функції foo(), чекаючи завершення промісу.
// Через 2 секунди проміс виконується (resolve(5)), і його значення (5) зберігається у змінній promiseValue.
// Код після await продовжує виконуватися.

//* Основні правила використання await

//* await можна використовувати тільки всередині асинхронних функцій (async).
//* await не працює самостійно без async, але async може існувати без await.
//* await призупиняє виконання функції, доки проміс не завершиться.
//* Якщо проміс успішний, await повертає його значення.
//* Якщо проміс відхилено, await генерує помилку, яку потрібно обробляти через try/catch.

// Взаємозв’язок async та await
// Асинхронна функція (async) завжди повертає проміс.
// Будь-яке значення, що повертається з asyncфункції, автоматично обгортається в Promise.resolve().
// Якщо функція не повертає значення, повернеться Promise.resolve(undefined).
// Оператор await дозволяє писати асинхронний код у зрозумілішому стилі, уникнувши вкладених .then() і покращуючи читабельність.
// todo Test:
// Що робить оператор await у коді?

// Виконує синхронну операцію
// Зупиняє виконання програми назавжди
// Призупиняє виконання коду на 10 секунд
//* Чекає завершення асинхронної операції та повертає її результат
// Result

// Саме так! Оператор await чекає завершення асинхронної операції та повертає її результат.

// Де можна використовувати оператор await?

// У будь-якому JavaScript коді
//* В асинхронних функціях (функціях, оголошених ключовим словом async)
// Тільки в умовних операторах (if/else)
// Тільки у функціях, оголошених як статичні
// Result

//* Абсолютно точно! Оператор await можна використовувати всередині асинхронних функцій.

//? HTTP-запити

// Використання async/await при роботі з HTTP-запитами робить код більш читабельним і простішим у розумінні.
// Перепишемо функцію fetchUsers з async/await. Спочатку розглянемо її у стандартному вигляді з axios і .then():

// const fetchUsers = () => {
// 	axios.get("https://jsonplaceholder.typicode.com/users")
// 	  .then(response => {
// 			console.log(response.data);
// 	  });
// };

// Додаємо async перед функцією:

// const fetchUsers = async () => {
// 	// ...
// };

// Використовуємо await, щоб зачекати завершення axios.get та отримати відповідь у змінну response:

// const fetchUsers = async () => {
// 	const response = await axios.get("https://jsonplaceholder.typicode.com/users");
// 	console.log(response.data);
// };

// Якщо отримані дані потрібно використовувати зовні, повертаємо їх із функції. Оскільки async-функція завжди повертає проміс, 
// обробляємо результат через .then():

// const fetchUsers = async () => {
// 	const response = await axios.get("https://jsonplaceholder.typicode.com/users");
// 	return response.data;
// };

// fetchUsers()
// 	.then(users => console.log(users));

// Чому async/await зручніше?
// Запит виконується без вкладених .then().
// Код виглядає послідовним, наче виконується синхронно.
// Легше читати та дебажити
// async/await робить роботу з HTTP-запитами зрозумілішою та зручнішою у порівнянні зі стандартним підходом на промісах.

//? Конструкція try…catch

// Конструкція try...catch використовується для обробки помилок, які можуть виникати під час виконання коду. Вона дозволяє:
// Запобігати аварійному завершенню програми.
// Логувати помилки та вживати відповідні заходи.
// Повідомляти користувача про помилку.


// try {
//   // Код, у якому можуть виникнути помилки
// } catch (error) {
//   // Код для обробки помилок, що виникли у блоці try
// }


// Як працює try...catch?

// Код у try виконується.
// Якщо виникає помилка, подальше виконання try зупиняється, і програма переходить у catch.
// Код у catch обробляє помилку, дозволяючи програмі продовжити роботу.
// Розглянемо приклад де ми намагаємося поділити число на нуль, що призводить до помилки. За допомогою try...catch можна 
// перехопити цю помилку та вивести повідомлення про неї.

// console.log("Before try...catch");

// try {
//   const result = 10 / 0;
//   console.log(result); // Цей рядок не виконається через помилку
// } catch (error) {
//   // Обробимо помилку
//   console.error(error.message);
// }

// console.log("After try...catch");

// Навіщо використовувати try...catch?

// Без try...catch помилка може зупинити виконання всього коду.
// З try...catch можна контролювати поведінку програми у разі помилки.

// Ця конструкція часто використовується у роботі з промісами, async/await, та HTTP-запитами для безпечного виконання 
// асинхронних операцій.
// todo test :

// Що робить конструкція try...catch?

// Перехоплює та виводить помилку в консоль.
// Автоматично виправляє помилки у твоєму коді.
//* Виконує код у блоці try, а якщо він викидає помилку, виконує код у блоці catch.
// Result

//* Звісно, так! Конструкція try...catch виконує код у блоці try, і якщо в цьому блоці виникає помилка (виняток), 
// вона перехоплюється, і виконується код у блоці catch.

//? Обробка помилок 

// При використанні async/await помилки можуть виникати, якщо проміс відхилено (наприклад, сервер не відповідає або повертає 
// помилку). Обробляти такі помилки можна двома способами:

// Всередині асинхронної функції за допомогою try...catch.
// Зовні через .catch() при виклику функції.

// 1. Обробка помилок у самій функції (try...catch)

// Якщо результат функції не використовується в зовнішньому коді, помилку потрібно обробляти прямо в її тілі.

// const fetchUsers = async () => {
//   try {
//     const response = await axios.get("https://jsonplaceholder.typicode.com/users");
//     console.log(response.data);
//   } catch (error) {
//     console.log("Помилка при отриманні користувачів:", error);
//   }
// };

// Якщо проміс відхилено, await генерує помилку, яка передається у catch.

// 2. Обробка помилок у .catch() зовні

// Якщо результат повертається з асинхронної функції, обробка помилки виконується у .catch() при виклику цієї функції.

// const fetchUsers = async () => {
//   const response = await axios.get("https://jsonplaceholder.typicode.com/users");
//   return response.data;
// };

// fetchUsers()
//   .then(users => console.log(users))
//   .catch(error => console.log("Помилка:", error));

// У цьому випадку помилка, згенерована await, відхиляє проміс, і вона перехоплюється у .catch().

// 3. Обробка помилок в іншій асинхронній функції (try...catch)

// Якщо асинхронна функція викликається в іншій async-функції, обробка відбувається через try...catch.

// const fetchUsers = async () => {
//   const response = await axios.get("https://jsonplaceholder.typicode.com/users");
//   return response.data;
// };

// const doStuff = async () => {
//   try {
//     const users = await fetchUsers();
//     console.log(users);
//   } catch (error) {
//     console.log("Помилка в doStuff:", error);
//   }
// };

// doStuff();

// Помилка fetchUsers() буде перехоплена у catch функції doStuff().

// Коли який підхід використовувати?

// try...catch у функції – коли потрібно обробити помилку локально.
// .catch() зовні – коли асинхронна функція повертає проміс, а обробку виконує зовнішній код.
// try...catch у asyncфункції – коли виклик відбувається всередині іншої асинхронної функції.

//!   Пагінація

//? Колекції та групи

// База даних може містити тисячі записів у колекціях. Наприклад, у застосунку для перегляду постів чи товарів нераціонально завантажувати всю 
// колекцію під час кожного запиту.
// Чому не варто завантажувати всю колекцію?

// Велике навантаження на браузер – обробка великої кількості даних вимагає значних обчислювальних ресурсів.
// Тривалий час завантаження – чим більше даних, тим довше вони передаються мережею.
// Навантаження на сервер – повернення тисяч записів у відповіді сповільнює роботу бекенду.
// Користувач не потребує всіх даних одразу – зазвичай йому достатньо бачити частину результатів.

// Уявімо, що сервер my-api.com містить колекцію постів за ресурсом /posts. Якщо колекція містить 10 000 постів, повернення всієї бази у відповіді 
// буде надто затратним.

// Для розв’язання цих проблем використовують пагінацію – сервер повертає не всі записи, а лише певну частину, наприклад, по 10 або 20 записів при
// кожному GET-запиті. Пагінацію реалізують на сервері та використовують її на клієнтському боці завдяки спеціальним параметрам у запиті.
// Для прикладу роботи з колекцією даних ми використовуємо публічний JSONPlaceholder API та отримуємо масив постів з ендпоінта /posts.

// const fetchPosts = async() => {
// 	const response = await axios.get("https://jsonplaceholder.typicode.com/posts");
// 	console.log("Posts: ", response.data);
// };

// У цьому коді fetchPosts() отримує всі пости, що може бути неефективним. У наступних розділах розглянемо реалізацію пагінації, щоб оптимізувати запити.


//? Кількість елементів у відповіді

// При запиті до сервера можна обмежити кількість отриманих записів, щоб не завантажувати всю колекцію одразу.
// Як визначається кількість елементів? Це залежить від параметра, який визначає кількість записів у відповіді.
// Найчастіше використовуються назви: per_page, limit, _limit, count.
// Стандартні назви параметрів пагінації не існують, тому їхні імена залежать від розробника серверної частини.
// У нашему прикладі використовується параметр per_page.
// Якщо колекція /posts містить 10 000 елементів, запит з per_page**=5** поверне лише перші 5 записів. Якщо передати від’ємне значення або число більше, ніж доступна кількість записів, сервер може:\
// Проігнорувати параметр та повернути значення за замовчуванням.
// Видати помилку 400 (BAD REQUEST).
// JSONPlaceholder API підтримує пагінацію. Кількість елементів у відповіді контролюється параметром _limit.

// const fetchPosts = async() => {
// 	const response = await axios.get("https://jsonplaceholder.typicode.com/posts?_limit=5");
// 	console.log("Posts: ", response.data);
// };

// Що відбувається в цьому коді?
// Сервер повертає лише 5 елементів із колекції /posts.
// Це дозволяє оптимізувати запити та не перевантажувати інтерфейс зайвими даними.
// Обмеження кількості записів у відповіді є основою пагінації, яка допомагає зменшити навантаження на сервер і покращити продуктивність вебзастосунку.
// Спробуй змінити значення параметра _limit у цьому прикладі і вивчи відповідь сервера в інтерфейсі та на вкладці Network.
// const fetchPostsBtn = document.querySelector(".btn");
// const postList = document.querySelector(".posts");

// fetchPostsBtn.addEventListener("click", async () => {
//   try {
//     const posts = await fetchPosts();
//     renderPosts(posts);
//   } catch (error) {
//     console.log(error);
//   }
// });

// async function fetchPosts() {
//   // Change the number of items in the group here
//   const response = await axios.get(
//     "https://jsonplaceholder.typicode.com/posts?_limit=5"
//   );
//   return response.data;
// }

// function renderPosts(posts) {
//   const markup = posts
//     .map(({ id, title, body, userId }) => {
//       return `<li>
//           <h2 class="post-title">${title.slice(0, 30)}</h2>
//           <p><b>Post id</b>: ${id}</p>
//           <p><b>Author id</b>: ${userId}</p>
//           <p>${body}</p>
//         </li>`;
//     })
//     .join("");
//   postList.innerHTML = markup;
// }


//? Номер групи елементів

// Після встановлення бажаної кількості елементів у відповіді ми завжди отримуватимемо першу групу записів (наприклад, перші 5 елементів). Щоб отримати наступні 
// групи, використовується другий параметр пагінації, який визначає номер групи (або "сторінки").
// Другий параметр пагінації контролює зміщення всередині колекції, тобто номер групи елементів, яку ми хочемо отримати. Якщо сервер реалізує пагінацію, 
// то значення цього параметра за замовчуванням дорівнює одиниці — перша група або «сторінка» елементів. У нашому прикладі він називається page.
// Змінюючи значення параметра page, ми уточнюємо серверу, яку наступну групу елементів ми хочемо отримати, і так продовжуємо, доки в колекції не закінчаться 
// елементи. Якщо вказати від'ємне значення або значення, більше за кількість груп у колекції, відповідь сервера залежить від його реалізації.
// У JSONPlaceholder API параметр, що контролює групу елементів, називається _page.

// const fetchPosts = async() => {
// 	const response = await axios.get("https://jsonplaceholder.typicode.com/posts?_limit=5&_page=3");
// 	console.log("Posts: ", response.data);
// };

// Цей код використовує _limit=5 для обмеження кількості елементів у відповіді та отримує третю групу записів (записи 11–15).
// Спробуй змінювати значення параметра _page в цьому прикладі і досліджуй відповідь сервера в інтерфейсі та на вкладці Network.

// todo Приклад 12-2

// const fetchPostsBtn = document.querySelector(".btn");
// const postList = document.querySelector(".posts");

// fetchPostsBtn.addEventListener("click", async () => {
//   try {
//     const posts = await fetchPosts();
//     renderPosts(posts);
//   } catch (error) {
//     console.log(error);
//   }
// });

// async function fetchPosts() {
//   const params = new URLSearchParams({
//     _limit: 5,
//     // Change the group number here
//     _page: 3
//   });

//   const response = await axios.get(
//     `https://jsonplaceholder.typicode.com/posts?${params}`
//   );
//   return response.data;
// }

// function renderPosts(posts) {
//   const markup = posts
//     .map(({ id, title, body, userId }) => {
//       return `<li>
//           <h2 class="post-title">${title.slice(0, 30)}</h2>
//           <p><b>Post id</b>: ${id}</p>
//           <p><b>Author id</b>: ${userId}</p>
//           <p class="post-body">${body}</p>
//         </li>`;
//     })
//     .join("");
//   postList.innerHTML = markup;
// }

// Щоб визначити, коли в колекції закінчаться елементи, і повідомити про це користувача, сервер може повертати не лише масив елементів, а й додаткові метадані.
// Кількість доступних груп – залежить від значення параметра per_page (або _limit).
// Загальну кількість елементів – у такому разі фронтенд-розробник сам обчислює кількість сторінок.
// Якщо сервер повертає загальну кількість елементів, можна розрахувати максимальну кількість сторінок:
// total_pages = total_items / per_page
// На жаль, JSONPlaceholder API не надає жодної інформації про загальну кількість елементів або сторінок, тому цей функціонал доведеться реалізувати самостійно 
// на основі отриманих даних.

//? Прийом «Завантажити ще»

// Щоб динамічно змінювати номер групи при кожному запиті, використовують такий підхід:
// Оголошуємо змінну page та встановлюємо її значення 1 (перша група елементів).
// Після кожного успішного запиту збільшуємо page на 1.
// Використовуємо це значення у параметрах запиту, щоб отримати наступну групу.

// 🔗 Приклад реалізації у CodePen:
// todo Приклад 12- 4
// const fetchPostsBtn = document.querySelector(".btn");
// const postList = document.querySelector(".posts");

// // Controls the group number
// let page = 1;
// // Controls the number of items in the group
// let perPage = 10;

// fetchPostsBtn.addEventListener("click", async () => {
//   try {
//     const posts = await fetchPosts();
//     renderPosts(posts);
//     // Increase the group number
//     page += 1;

//     // Replace button text after first request
//     if (page > 1) {
//       fetchPostsBtn.textContent = "Fetch more posts";
//     }
//   } catch (error) {
//     console.log(error);
//   }
// });

// async function fetchPosts() {
//   const params = new URLSearchParams({
//     _limit: perPage,
//     _page: page
//   });

//   const response = await axios.get(
//     `https://jsonplaceholder.typicode.com/posts?${params}`
//   );
//   return response.data;
// }

// function renderPosts(posts) {
//   const markup = posts
//     .map(({ id, title, body, userId }) => {
//       return `<li>
//           <h2 class="post-title">${title.slice(0, 30)}</h2>
//           <p><b>Post id</b>: ${id}</p>
//           <p><b>Author id</b>: ${userId}</p>
//           <p class="post-body">${body}</p>
//         </li>`;
//     })
//     .join("");
//   postList.insertAdjacentHTML("beforeend", markup);
// }
// Що відбувається при натисканні кнопки?

// Завантажується перша група елементів (page=1).
// Текст на кнопці змінюється, а вона переміщується під список постів.
// Коли користувач прокручує сторінку та натискає кнопку знову, завантажується наступна група (page=2), яка додається до списку.
// Такий підхід дозволяє динамічно підвантажувати контент та уникати перевантаження сторінки великою кількістю даних одразу.

//? Перевірка кінця колекції

// Якщо під час натискання кнопки "Fetch posts" нові записи більше не завантажуються, це означає, що користувач досяг кінця колекції. У такому разі необхідно відобразити сповіщення.
// Оскільки JSONPlaceholder API не надає інформацію про кількість сторінок, ми реалізуємо перевірку на фронтенді.
// Отримуємо загальну кількість записів у колекції (наприклад, 100).
// Ділимо її на кількість елементів у групі (limit).
// Розраховуємо загальну кількість сторінок.
// Перед кожним новим запитом перевіряємо, чи є ще сторінки для завантаження.

// 🔗 Живий приклад у CodePen:
// todo Приклад 12-5
// const fetchPostsBtn = document.querySelector(".btn");
// const postList = document.querySelector(".posts");

// // Controls the group number
// let page = 1;
// // Controls the number of items in the group
// let limit = 40;
// // In our case total number of pages is calculated on frontend
// const totalPages = Math.ceil(100 / limit);

// fetchPostsBtn.addEventListener("click", async () => {
//   // Check the end of the collection to display an alert
//   if (page > totalPages) {
//     return iziToast.error({
//       position: "topRight",
//       message: "We're sorry, there are no more posts to load"
//     });
//   }

//   try {
//     const posts = await fetchPosts();
//     renderPosts(posts);
//     // Increase the group number
//     page += 1;

//     // Replace button text after first request
//     if (page > 1) {
//       fetchPostsBtn.textContent = "Fetch more posts";
//     }
//   } catch (error) {
//     console.log(error);
//   }
// });

// async function fetchPosts() {
//   const params = new URLSearchParams({
//     _limit: limit,
//     _page: page
//   });

//   const response = await axios.get(
//     `https://jsonplaceholder.typicode.com/posts?${params}`
//   );
//   return response.data;
// }

// function renderPosts(posts) {
//   const markup = posts
//     .map(({ id, title, body, userId }) => {
//       return `<li>
//           <h2 class="post-title">${title.slice(0, 30)}</h2>
//           <p><b>Post id</b>: ${id}</p>
//           <p><b>Author id</b>: ${userId}</p>
//           <p class="post-body">${body}</p>
//         </li>`;
//     })
//     .join("");
//   postList.insertAdjacentHTML("beforeend", markup);
// }

//? Розрахунок кількості сторінок

// Ми додали перевірку кінця колекції на фронтенді, оскільки JSONPlaceholder API не реалізує цей функціонал на бекенді. 
// У нашому випадку достатньо розділити загальну кількість елементів у колекції (100) на кількість елементів в одній групі.

// Кількість елементів у групі
// let limit = 30;
// Загальна кількість сторінок у колекції
// const totalPages = Math.ceil(100 / limit);

// Перевірка перед новим запитом
// Перед кожним наступним запитом порівнюємо поточний номер сторінки (page) із загальною кількістю (totalPages). Якщо всі записи завантажені, показуємо сповіщення замість запиту. Для цього ми використовуємо бібліотеку iziToast.

// if (page > totalPages) {
//   return iziToast.error({
//     position: "topRight",
//     message: "We're sorry, there are no more posts to load"
//   });
// }

// Це схоже на випадок, коли бекенд повертає не кількість доступних сторінок, а загальну кількість елементів у колекції.

// Контролює кількість елементів в групі
// let limit = 30;
// Кількість груп в колекції
// const totalPages = Math.ceil(100 / limit);

// Що це дає?

// Уникнення зайвих запитів до сервера, якщо всі записи вже отримані.
// Покращення UX – користувач розуміє, що завантаження завершено.

// Такий підхід корисний для API, які не повертають загальну кількість сторінок у відповідях.

const fetchPosts = async() => {
	const response = await axios.get("https://jsonplaceholder.typicode.com/posts?_limit=5");
	console.log("Posts: ", response.data);
};

fetchPosts();
